# Marker Region Fixture Index

This workspace is intentionally small. It is the quick sanity fixture for marker
promotion, region nesting, and overlap behavior without combining every stressor
in one graph.

Use the focused fixtures under `../canvas-layout-cases/` for diagnosis:

- `nested-regions`: explicit and inferred region nesting.
- `promoted-markers`: covered nodes, multiple markers on one node, one loader.
- `overlap-regions`: identical and partial region overlaps.
- `cross-region-links`: sibling and cousin crossings without marker noise.

Quick review URL:

```text
http://127.0.0.1:26339/?workspace=/Users/olafbobryk/Documents/Code/Personal/2026/codex-orchestrator-dashboard/fixtures/marker-region-stress
```
