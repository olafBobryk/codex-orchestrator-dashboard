# Codex Orchestrator Public Example

This folder is a standalone static Next.js example for public deployment.

It is intentionally separate from the real dashboard runtime. The production
sidecar reads local filesystem state from project `.codex-orchestration/`
folders; this example uses hardcoded sample data only.
